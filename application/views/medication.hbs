<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <title>Medication Details</title>
  <link rel="stylesheet" href="/css/medication.css">
</head>

<body>
  <nav>
    <div class="logo">
      medRec
    </div>
    <div class="loginSignup">
      <a href="/profile" >Hello, {{name}} </a>
      <button class="logoutButton" onclick="location.href='/index'">Logout</button>
    </div>
  </nav>
  <main>
    <div class="container">
      {{#each patientMedication as |mypatientMedication|}}
      <div class="medicinedetails">
        <div class = "medicine" style="background-color: #bbbbbb;">
          <h3> {{mypatientMedication.disease}} </h3><br>
        </div>
        <div class = "disease">
          <h4 style="background-color: #999999;">{{mypatientMedication.medicine}}</h4><br>
        </div>
        {{#each mypatientMedication.times as |time|}}
        <p>{{time}}</p><br>
        {{/each}}
      </div>
      {{/each}}
    </div>
  </main>
</body>
</html>
